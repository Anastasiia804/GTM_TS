# 🚀 Quick Start Guide - Custom Tag Manager

## Быстрый старт за 5 минут

### 1️⃣ Установка зависимостей

```bash
npm install
```

### 2️⃣ Настройка окружения

Создайте файл `.env`:
```bash
PORT=3000
JWT_SECRET=your-secret-key-change-this-in-production
```

### 3️⃣ Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

### 4️⃣ Вход в админ-панель

Откройте браузер: `http://localhost:3000`

**Учетные данные по умолчанию:**
- Email: `admin@example.com`
- Password: `admin123`

⚠️ **ВАЖНО:** Измените эти данные в продакшене!

### 5️⃣ Создание первого контейнера

1. После входа нажмите кнопку "+" в разделе Containers
2. Введите:
   - **Name:** "My First Container"
   - **Domain:** `https://your-website.com` (опционально)
3. Нажмите "Create"

### 6️⃣ Скопируйте код установки

После создания контейнера вы увидите код для установки:
```html
<script src="http://localhost:3000/container.js?id=CTM-XXXXXXXXXX"></script>
```

### 7️⃣ Установка на сайт рекламодателя

Рекламодатель должен вставить этот код в `<head>` секцию своего сайта:

```html
<!DOCTYPE html>
<html>
<head>
  <!-- Ваш Tag Manager -->
  <script src="http://localhost:3000/container.js?id=CTM-XXXXXXXXXX"></script>
</head>
<body>
  <!-- Контент -->
</body>
</html>
```

### 8️⃣ Добавление первого тега

#### Пример: Facebook Pixel

1. Нажмите "Add Tag"
2. Заполните форму:
   - **Name:** "Facebook Pixel"
   - **Type:** "Script"
   - **Script URL:** `https://connect.facebook.net/en_US/fbevents.js`
3. Нажмите "Create"

4. Добавьте триггер:
   - Нажмите "Add Trigger"
   - **Type:** "Page View"
   - Нажмите "Add Trigger"

5. Добавьте инициализацию (второй тег):
   - **Name:** "FB Pixel Init"
   - **Type:** "Custom JavaScript"
   - **Code:**
   ```javascript
   fbq('init', 'YOUR_PIXEL_ID');
   fbq('track', 'PageView');
   ```
   - **Trigger:** "Page View"

### 9️⃣ Тестирование

Откройте тестовую страницу: `http://localhost:3000/test-page.html`

Эта страница поможет вам:
- Проверить загрузку контейнера
- Тестировать события
- Отлаживать теги
- Просматривать dataLayer

### 🔟 Отправка событий с сайта

На сайте рекламодателя можно отправлять события:

```javascript
// Событие покупки
window.dataLayer.push({
  event: 'purchase',
  transaction_id: '12345',
  value: 99.99,
  currency: 'USD',
  items: [
    {
      item_id: 'PROD-123',
      item_name: 'Product Name',
      price: 99.99,
      quantity: 1
    }
  ]
});

// Добавление в корзину
window.dataLayer.push({
  event: 'add_to_cart',
  item_id: 'PROD-456',
  item_name: 'Another Product',
  price: 49.99
});

// Любое кастомное событие
window.dataLayer.push({
  event: 'custom_event_name',
  custom_parameter: 'value'
});
```

---

## 📋 Популярные сценарии использования

### Сценарий 1: Трекинг конверсий

**Шаг 1:** Создайте тег для базового пикселя
```javascript
// Тип: Script
// URL: https://partner.com/pixel.js
// Триггер: Page View
```

**Шаг 2:** Создайте тег для конверсий
```javascript
// Тип: Custom JavaScript
// Триггер: Custom Event "purchase"
partnerPixel.track('conversion', {
  order_id: data.transaction_id,
  amount: data.value,
  currency: data.currency
});
```

**Шаг 3:** Отправьте событие на странице благодарности
```html
<script>
  window.dataLayer.push({
    event: 'purchase',
    transaction_id: '<?= $order_id ?>',
    value: <?= $order_total ?>,
    currency: 'USD'
  });
</script>
```

### Сценарий 2: Отслеживание кликов

**Шаг 1:** Создайте тег
```javascript
// Тип: Custom JavaScript
// Триггер: Click на элемент .buy-button
console.log('Buy button clicked!');
// Ваш код отслеживания
```

**Шаг 2:** Добавьте класс к кнопке
```html
<button class="buy-button">Купить</button>
```

### Сценарий 3: Условный запуск тега

```javascript
// Тип: Custom JavaScript
// Триггер: Custom Event "purchase"

// Запускать только для заказов больше 100
if (data.value && data.value > 100) {
  // Ваш код для премиум-заказов
  fbq('trackCustom', 'HighValuePurchase', {
    value: data.value
  });
}
```

---

## 🛠️ Отладка

### Включить режим отладки

```html
<!-- Добавьте &debug=true к URL -->
<script src="http://localhost:3000/container.js?id=CTM-XXX&debug=true"></script>
```

### Или через консоль браузера

```javascript
// Включить отладку
window.ctm.debug(true);

// Проверить состояние
window.ctm.getState();

// Просмотреть dataLayer
console.log(window.dataLayer);

// Отправить тестовое событие
window.ctm.push({ event: 'test_event' });
```

---

## 🚀 Деплой в продакшен

### Вариант 1: PM2 (рекомендуется)

```bash
# Установка PM2
npm install -g pm2

# Запуск
pm2 start server.js --name tag-manager

# Автозапуск при перезагрузке
pm2 startup
pm2 save

# Мониторинг
pm2 monit
```

### Вариант 2: Docker

```bash
# Сборка образа
docker build -t tag-manager .

# Запуск контейнера
docker run -d \
  -p 3000:3000 \
  -e JWT_SECRET=your-production-secret \
  --name tag-manager \
  tag-manager

# Или используйте docker-compose
docker-compose up -d
```

### Вариант 3: Heroku

```bash
# Создание приложения
heroku create your-tag-manager

# Настройка переменных
heroku config:set JWT_SECRET=your-production-secret

# Деплой
git push heroku main

# Открыть приложение
heroku open
```

---

## 🔒 Безопасность (ОБЯЗАТЕЛЬНО для продакшена!)

### 1. Измените JWT_SECRET

```bash
# В .env файле
JWT_SECRET=$(openssl rand -base64 32)
```

### 2. Настройте HTTPS

```bash
# Используйте Let's Encrypt или Cloudflare
# Nginx пример:
server {
    listen 443 ssl http2;
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://localhost:3000;
    }
}
```

### 3. Настройте CORS

В `server.js` обновите:
```javascript
app.use(cors({
  origin: ['https://yourdomain.com', 'https://admin.yourdomain.com'],
  credentials: true
}));
```

### 4. Измените учетные данные по умолчанию

В `server.js` замените:
```javascript
const database = {
  users: [
    {
      id: '1',
      email: 'your-email@example.com',  // ИЗМЕНИТЕ
      password: bcrypt.hashSync('your-secure-password', 10),  // ИЗМЕНИТЕ
      role: 'admin'
    }
  ]
}
```

---

## 📊 Мониторинг и аналитика

### Логирование

```javascript
// Добавьте Winston для логирования
npm install winston

// В server.js:
const winston = require('winston');
const logger = winston.createLogger({
  level: 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

// Используйте вместо console.log
logger.info('Container loaded', { containerId: 'CTM-XXX' });
```

### Метрики (Prometheus)

```javascript
// Добавьте prom-client
npm install prom-client

// Отслеживайте:
// - Количество запросов
// - Время ответа
// - Ошибки
// - Количество тегов
```

---

## 💡 Советы и лучшие практики

### 1. Производительность
- ✅ Используйте async для скриптов
- ✅ Загружайте тяжелые скрипты по требованию
- ✅ Кэшируйте container.js
- ✅ Минифицируйте код

### 2. Организация
- ✅ Используйте понятные имена для тегов
- ✅ Группируйте похожие теги
- ✅ Документируйте назначение каждого тега
- ✅ Тестируйте перед публикацией

### 3. Безопасность
- ✅ Регулярно обновляйте зависимости
- ✅ Используйте HTTPS
- ✅ Валидируйте все входные данные
- ✅ Логируйте все действия

### 4. Масштабирование
- ✅ Используйте базу данных (MongoDB/PostgreSQL)
- ✅ Настройте кэширование (Redis)
- ✅ Используйте CDN для container.js
- ✅ Load balancing для высоких нагрузок

---

## 🆘 Частые проблемы и решения

### Проблема: Контейнер не загружается

**Решение:**
```javascript
// Проверьте в консоли браузера:
console.log(window.ctm);  // Должен быть объект

// Проверьте сеть:
// Network tab → container.js → Status должен быть 200

// Проверьте ID контейнера:
// URL должен содержать правильный ?id=CTM-XXX
```

### Проблема: Теги не срабатывают

**Решение:**
```javascript
// 1. Включите debug режим
window.ctm.debug(true);

// 2. Проверьте триггеры
// Убедитесь что событие отправляется:
window.dataLayer.push({ event: 'test' });

// 3. Проверьте условия триггера
// URL должен совпадать, селекторы должны существовать
```

### Проблема: CORS ошибки

**Решение:**
```javascript
// В server.js добавьте домен:
app.use(cors({
  origin: ['http://localhost:3000', 'https://yourdomain.com'],
  credentials: true
}));
```

---

## 📚 Дополнительные ресурсы

- **README.md** - Полная документация
- **ARCHITECTURE.md** - Архитектура системы
- **AFFILIATE_SCRIPTS_GUIDE.md** - Примеры партнерских скриптов
- **test-page.html** - Тестовая страница
- **database-mongodb.js** - Реализация с MongoDB

---

## 🤝 Поддержка

Если возникли вопросы:
1. Проверьте документацию
2. Посмотрите примеры в AFFILIATE_SCRIPTS_GUIDE.md
3. Используйте test-page.html для отладки
4. Проверьте логи сервера

---

## 🎉 Поздравляем!

Теперь у вас есть полноценная система управления тегами!

Следующие шаги:
1. ✅ Добавьте первый контейнер
2. ✅ Настройте первый тег
3. ✅ Протестируйте на test-page.html
4. ✅ Установите на реальный сайт
5. ✅ Добавьте больше партнерских тегов
6. ✅ Настройте продакшен окружение

**Удачи в работе! 🚀**
