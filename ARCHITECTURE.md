# Архитектура Custom Tag Manager

## 📐 Общая архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                    Сайт рекламодателя                           │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  <script src="yourdomain.com/container.js?id=CTM-XXX">  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Container Script (container.js)              │  │
│  │  • Загружает конфигурацию                                │  │
│  │  • Обрабатывает триггеры                                 │  │
│  │  • Выполняет теги                                        │  │
│  │  • Работает с dataLayer                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     DataLayer                             │  │
│  │  window.dataLayer = [                                     │  │
│  │    { event: 'pageview' },                                │  │
│  │    { event: 'purchase', value: 99.99 }                   │  │
│  │  ]                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS Request
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Ваш сервер (yourdomain.com)                  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API Server (Express)                   │  │
│  │                                                            │  │
│  │  PUBLIC Endpoints:                                        │  │
│  │  • GET /container.js          - Скрипт контейнера        │  │
│  │  • GET /api/config/:id        - Конфигурация             │  │
│  │                                                            │  │
│  │  PROTECTED Endpoints (JWT):                               │  │
│  │  • POST /api/auth/login       - Авторизация              │  │
│  │  • GET /api/containers        - Список контейнеров       │  │
│  │  • POST /api/containers       - Создание контейнера      │  │
│  │  • GET/PUT/DELETE /api/tags   - Управление тегами        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     База данных                           │  │
│  │  • Пользователи                                          │  │
│  │  • Контейнеры                                            │  │
│  │  • Теги                                                  │  │
│  │  • Триггеры                                              │  │
│  │  • Аналитика (опционально)                              │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↑
                              │ HTTPS Request
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        Админ-панель                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  React UI (admin-panel.jsx)               │  │
│  │                                                            │  │
│  │  • Управление контейнерами                                │  │
│  │  • Создание/редактирование тегов                         │  │
│  │  • Настройка триггеров                                   │  │
│  │  • Копирование кода установки                            │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Поток данных при загрузке страницы

```
1. Браузер загружает страницу рекламодателя
   ↓
2. Выполняется <script src="yourdomain.com/container.js?id=CTM-XXX">
   ↓
3. container.js делает запрос к API:
   GET /api/config/CTM-XXX
   ↓
4. Сервер возвращает конфигурацию:
   {
     containerId: "CTM-XXX",
     tags: [
       {
         id: "TAG-1",
         type: "script",
         src: "https://facebook.com/pixel.js",
         triggers: [{ type: "pageview" }]
       }
     ]
   }
   ↓
5. container.js обрабатывает триггеры и выполняет теги:
   • Pageview триггеры → сразу
   • DOM Ready триггеры → после DOMContentLoaded
   • Custom Events → при вызове dataLayer.push()
   ↓
6. Теги загружают партнерские скрипты:
   • Facebook Pixel
   • Google Analytics
   • Affiliate пиксели
```

## 🎯 Жизненный цикл тега

```
┌─────────────────┐
│  Tag Created    │ Администратор создает тег в админке
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Triggers Set   │ Администратор настраивает триггеры
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Config Stored  │ Конфигурация сохраняется в БД
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Page Load      │ Пользователь заходит на сайт
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Container Load │ container.js загружает конфигурацию
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ Trigger Check   │ Проверка условий триггера
└────────┬────────┘
         │
         ├─ Условие не выполнено → Ожидание
         │
         ├─ Условие выполнено → Выполнение тега
         │
         ↓
┌─────────────────┐
│   Tag Execute   │ Загрузка и выполнение партнерского скрипта
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ Track Analytics │ (Опционально) Логирование в БД
└─────────────────┘
```

## 📊 Модель данных

### Containers
```javascript
{
  id: "CTM-1234567890-abc123",
  name: "My Website Container",
  domain: "https://example.com",
  description: "Main site container",
  userId: "user_id_123",
  enabled: true,
  version: 5,
  createdAt: "2024-01-01T00:00:00Z",
  updatedAt: "2024-01-15T10:30:00Z"
}
```

### Tags
```javascript
{
  id: "TAG-1234567890-xyz789",
  containerId: "CTM-1234567890-abc123",
  name: "Facebook Pixel",
  type: "script",  // "html" | "script" | "image" | "custom"
  
  // Для type: "script"
  src: "https://connect.facebook.net/en_US/fbevents.js",
  async: true,
  
  // Для type: "html"
  html: "<div>HTML content</div>",
  
  // Для type: "image"
  src: "https://pixel.com/track.gif?id={{transaction_id}}",
  
  // Для type: "custom"
  code: "console.log('Custom JS');",
  
  attributes: {
    "data-cfasync": "false"
  },
  
  fireOnce: true,
  enabled: true,
  
  triggers: [
    {
      id: "TRG-123",
      type: "pageview"
    },
    {
      id: "TRG-456",
      type: "custom_event",
      eventName: "purchase"
    }
  ],
  
  createdAt: "2024-01-01T00:00:00Z",
  updatedAt: "2024-01-10T15:20:00Z"
}
```

### Triggers
```javascript
// Pageview trigger
{
  id: "TRG-001",
  type: "pageview"
}

// Custom event trigger
{
  id: "TRG-002",
  type: "custom_event",
  eventName: "purchase"
}

// URL match trigger
{
  id: "TRG-003",
  type: "url_match",
  url: "/checkout",
  matchType: "contains"  // "equals" | "contains" | "regex"
}

// Click trigger
{
  id: "TRG-004",
  type: "click",
  selector: ".buy-button"
}

// Element visibility trigger
{
  id: "TRG-005",
  type: "element_visibility",
  selector: "#product-section"
}

// DOM Ready trigger
{
  id: "TRG-006",
  type: "dom_ready"
}

// Window Load trigger
{
  id: "TRG-007",
  type: "window_load"
}
```

## 🔐 Безопасность

### 1. Уровни доступа
```
┌─────────────────────────────────────────┐
│           Public Access                 │
│  • container.js (любой может загрузить) │
│  • GET /api/config/:id                  │
└─────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│         Authenticated Access            │
│  • JWT Token Required                   │
│  • Управление контейнерами              │
│  • CRUD операции с тегами               │
└─────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│            Admin Access                 │
│  • role: "admin"                        │
│  • Доступ ко всем контейнерам          │
│  • Управление пользователями           │
└─────────────────────────────────────────┘
```

### 2. Защита от XSS
- Helmet.js для заголовков безопасности
- Валидация всех входных данных
- Санитизация HTML в тегах (опционально)
- CSP заголовки

### 3. Rate Limiting
```javascript
// Для публичных endpoint'ов
configLimiter: 60 запросов/минуту

// Для API endpoint'ов
apiLimiter: 100 запросов/15 минут
```

## ⚡ Производительность

### Кэширование
```
┌─────────────────────────────────────────┐
│           Browser Cache                 │
│  container.js: Cache-Control: 1 hour    │
└─────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│            CDN Cache                    │
│  Статические файлы через CDN            │
└─────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│         Server Cache (Redis)            │
│  Конфигурации контейнеров: 5 минут     │
└─────────────────────────────────────────┘
```

### Оптимизация загрузки
1. **Async loading** - container.js загружается асинхронно
2. **Lazy execution** - теги выполняются по триггерам
3. **Batch requests** - минимизация HTTP запросов
4. **Compression** - Gzip/Brotli для всех ответов

## 🔄 Масштабирование

### Горизонтальное масштабирование
```
                    Load Balancer (Nginx)
                           │
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
    Server 1           Server 2           Server 3
        │                  │                  │
        └──────────────────┼──────────────────┘
                           ↓
                    Shared Database
                   (MongoDB/PostgreSQL)
                           │
                           ↓
                      Redis Cache
```

### Vertical масштабирование
- Увеличение ресурсов сервера
- Оптимизация запросов к БД
- Индексы для быстрого поиска

## 📈 Мониторинг

### Ключевые метрики
1. **Container Load Time** - время загрузки конфигурации
2. **Tag Execution Rate** - процент успешных выполнений
3. **Error Rate** - количество ошибок
4. **API Response Time** - время ответа API
5. **Database Query Time** - время запросов к БД

### Логирование
```javascript
// Структура лога
{
  timestamp: "2024-01-15T10:30:00Z",
  level: "info",  // "error" | "warn" | "info" | "debug"
  containerId: "CTM-XXX",
  tagId: "TAG-YYY",
  event: "tag_executed",
  duration: 150,  // ms
  success: true,
  metadata: {
    url: "https://example.com/page",
    userAgent: "Mozilla/5.0..."
  }
}
```

## 🚀 Расширения и интеграции

### Возможные расширения:
1. **Visual Editor** - WYSIWYG редактор триггеров
2. **Version Control** - Git-подобная система версий
3. **A/B Testing** - Тестирование вариантов тегов
4. **Template Library** - Библиотека готовых шаблонов
5. **Webhook Integration** - Уведомления о событиях
6. **Multi-workspace** - Поддержка нескольких команд
7. **Scheduled Tags** - Теги по расписанию
8. **Geo-targeting** - Теги по географии
9. **Device-targeting** - Теги по устройствам
10. **Consent Management** - Управление согласиями (GDPR)

### Интеграции:
- **Slack** - Уведомления об изменениях
- **GitHub** - Экспорт/импорт конфигураций
- **Zapier** - Автоматизация workflow
- **DataDog** - Мониторинг и аналитика
- **Sentry** - Отслеживание ошибок

## 💾 Backup и восстановление

### Стратегия резервного копирования
```
Daily Backups
    ↓
Weekly Full Backups
    ↓
Monthly Archives
    ↓
Point-in-time Recovery
```

### Что бэкапится:
- База данных (полностью)
- Конфигурации контейнеров
- История изменений
- Пользовательские данные

## 🔧 DevOps Pipeline

```
┌─────────────┐
│  Git Push   │
└──────┬──────┘
       ↓
┌─────────────┐
│   CI/CD     │ GitHub Actions / GitLab CI
│  • Lint     │
│  • Test     │
│  • Build    │
└──────┬──────┘
       ↓
┌─────────────┐
│   Deploy    │
│  • Staging  │ Тестирование
│  • Review   │
└──────┬──────┘
       ↓
┌─────────────┐
│ Production  │ Zero-downtime deployment
│  • Blue-    │
│    Green    │
└─────────────┘
```

## 📱 Мобильная поддержка

Container.js полностью поддерживает мобильные браузеры:
- iOS Safari
- Chrome Mobile
- Samsung Internet
- Opera Mobile

## 🌍 Мультиязычность

Админ-панель может быть расширена для поддержки:
- Русский (ru)
- English (en)
- Español (es)
- Deutsch (de)
- 中文 (zh)

---

Эта архитектура обеспечивает:
✅ Масштабируемость
✅ Безопасность
✅ Производительность
✅ Гибкость
✅ Простоту использования
